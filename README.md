# wenyulu21.github.io

陆汶雨的 GitHub Pages 个人主页。

## 本地预览

直接用浏览器打开 `index.html` 即可预览。也可以在当前目录启动一个静态服务器：

```powershell
python -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 发布到 GitHub Pages

将本目录内容推送到 GitHub 仓库 `wenyulu21.github.io` 的默认分支。GitHub Pages 会自动使用根目录的静态文件作为个人主页。
